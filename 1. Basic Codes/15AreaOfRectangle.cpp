#include<iostream>

using namespace std;

int main()
{
    int length,breadth;

    cout<<"Enter the length of the length of rectangle : ";
    cin>>length;

    cout<<"Enter the length of the breadth of rectangle : ";
    cin>>breadth;

    int area = length*breadth;

    cout<<"The area of the rectangle is : "<<area;
}