#include<iostream>

using namespace std;

int main()
{
    int a,b;

    cout<<"Enter the two number : ";
    cin>>a>>b;

    if(a<b)
    {
        cout<<a<<" is minimum of the two numbers.";
    }

    else
    {
        cout<<b<<" is minimum of the two numbers.";
    }
}